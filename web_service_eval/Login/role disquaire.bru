meta {
  name: role disquaire
  type: http
  seq: 6
}

post {
  url: http://localhost:3000/v1/api/roles/
  body: json
  auth: inherit
}

body:json {
  {
    "name": "disquaire",
    "description": "Can list and edit vinyls and groups",
    "authorizations": [
      {
        "ressource": "vinyls",
        "permissions": {
          "full": ["GET", "PATCH"]
        }
      },
      {
        "ressource": "groups",
        "permissions": {
          "full": ["GET", "PATCH"]
        }
      }
    ]
  }
  
}

settings {
  encodeUrl: true
  timeout: 0
}
