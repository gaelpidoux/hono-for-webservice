meta {
  name: role manager
  type: http
  seq: 11
}

post {
  url: http://localhost:3000/v1/api/roles/
  body: json
  auth: inherit
}

body:json {
  {
    "name": "admineval",
    "description": "Full access",
    "authorizations": [
      {
        "ressource": "vinyls",
        "permissions": {
          "full": ["create", "read", "update", "delete"]
        }
      },
      {
        "ressource": "groups",
        "permissions": {
          "full": ["create", "read", "update", "delete"]
        }
      }
    ]
  }
  
}

settings {
  encodeUrl: true
  timeout: 0
}
